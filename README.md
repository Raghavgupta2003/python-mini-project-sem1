# 🎲 Random Number Properties Identifier

This project is a simple Python program that **generates a random number within a user-defined range** and displays its mathematical properties such as:

* Even or Odd
* Positive or Negative
* Prime or Composite

---

## 📌 Features

* User inputs the starting and ending range.
* Program generates a random number within the range.
* Determines and prints:

  * Whether the number is **even or odd**
  * Whether it is **positive or negative**
  * Whether it is **prime or composite**

---

## ▶️ How to Run
1. Install Python 3.x
2. Save the script as `identying property of a number.py`
3. Run the script:
```

python identying property of a number.py

```
4. Enter the range when prompted.

---
